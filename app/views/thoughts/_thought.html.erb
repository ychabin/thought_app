<div class="card__body">
    <%= link_to thought.title, thought_path(thought.id), class: :card__title%>
    
    <%= link_to "by#{thought.user.name}", user_path(thought.user), class: :card__user %>
</div>